{
  "name": "open-claude-cowork",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "dev": "NODE_ENV=development electron .",
    "start": "electron .",
    "server": ".venv/bin/python -m uvicorn server.app:app --host 127.0.0.1 --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "Electron chat app (Python FastAPI backend + Claude Agent SDK / OpenCode)",
  "dependencies": {},
  "devDependencies": {
    "electron": "^39.2.7",
    "electron-reload": "^2.0.0-alpha.1"
  }
}
